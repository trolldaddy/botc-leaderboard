(()=>{
  const $=(id)=>document.getElementById(id), esc=(v)=>String(v??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const state={items:[],active:null,loaded:false,roleJson:null,artworkCandidates:[],activeArtworkId:"",artworkSelection:{front:"",back:"",logo:""}};
  const customTypes=[['townsfolk','鎮民'],['outsider','外來者'],['minion','爪牙'],['demon','惡魔'],['traveller','旅行者'],['loric','奇遇角色'],['fabled','傳奇角色'],['jinx','相剋規則'],['special','其他']];
  const scriptCategories=['\u5b98\u65b9\u5287\u672c','\u5b98\u6df7\u5287\u672c','\u90e8\u5206\u539f\u5275','\u5b8c\u5168\u539f\u5275'];
  const categoryOptions=(selected)=>'<option value="">&#35531;&#36984;&#25799;&#20998;&#39006;</option>'+scriptCategories.map((value)=>`<option value="${value}" ${value===selected?'selected':''}>${value}</option>`).join('');
  const typeLabel=(value)=>customTypes.find(([key])=>key===value)?.[1]||'其他';
  const typeOptions=(selected)=>customTypes.map(([key,label])=>`<option value="${key}" ${key===selected?'selected':''}>${label}</option>`).join('');
  async function request(url,options={}){const response=await fetch(url,{credentials:'include',headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.detail||('HTTP '+response.status));return data;}
  function switchMode(mode){document.querySelectorAll('[data-role-mode]').forEach((el)=>el.hidden=mode!=='roles');$('knowledge-article-admin').hidden=mode!=='articles';$('script-admin').hidden=mode!=='scripts';if(mode==='scripts'&&!state.loaded)refresh();}
  function renderList(){const host=$('script-admin-list');host.innerHTML=state.items.map((x)=>`<button class="article-admin-item ${state.active?.id===x.id?'is-active':''}" data-script-id="${x.id}"><strong>${esc(x.name_zh_tw)}</strong><small>${esc(x.author_name||'作者待補')} · ${x.role_count+x.custom_role_count} 個條目${x.is_public?' · 公開':' · 內部'}</small></button>`).join('')||'<div class="role-admin-empty">沒有劇本。</div>';host.querySelectorAll('[data-script-id]').forEach((b)=>b.onclick=()=>load(Number(b.dataset.scriptId)));}
  async function refresh(){const status=$('script-admin-status');status.textContent='正在讀取劇本…';try{const data=await request('/api/admin/scripts?q='+encodeURIComponent($('script-admin-search').value.trim()));state.items=data.items||[];state.loaded=true;status.textContent=`共 ${state.items.length} 套劇本`;renderList();}catch(e){status.textContent='讀取失敗：'+e.message;}}
  const field=(label,id,value,wide=false,rich=false)=>`<label class="script-admin-field ${wide?'wide':''}"><span>${label}</span><textarea id="${id}" class="form-control dark-input" ${rich?'data-rich-text':''}>${esc(value)}</textarea></label>`;
  const artworkKinds=[['front','正面'],['back','背面'],['logo','Logo']];
  const artworkIcon=(kind)=>({front:'<i class="fas fa-file-image" aria-hidden="true"></i>',back:'<i class="fas fa-clone" aria-hidden="true"></i>',logo:'<i class="fas fa-copyright" aria-hidden="true"></i>'}[kind]||'');
  function artworkPicker(candidates,selection){if(!(candidates||[]).length)return '<div class="role-admin-empty">這篇文章沒有可讀取的候選圖片；請改用右上方手動上傳。</div>';if(!candidates.some((item)=>item.id===state.activeArtworkId))state.activeArtworkId=candidates[0].id;const current=candidates.find((item)=>item.id===state.activeArtworkId)||candidates[0];const currentKind=artworkKinds.find(([kind])=>selection[kind]===current.id)?.[0]||'';return `<div class="script-artwork-workbench"><section class="script-artwork-viewer"><div class="script-artwork-stage"><img src="${esc(current.url)}" alt="目前瀏覽的候選圖片"></div><div class="script-artwork-assign" aria-label="指定圖片用途">${artworkKinds.map(([kind,label])=>`<button type="button" class="${currentKind===kind?'is-selected':''}" data-artwork-kind="${kind}">${artworkIcon(kind)}<span>${label}</span></button>`).join('')}</div></section><aside class="script-artwork-list" aria-label="候選圖片清單">${candidates.map((item,index)=>{const kind=artworkKinds.find(([key])=>selection[key]===item.id)?.[0]||'';return `<button type="button" class="script-artwork-list-item ${item.id===current.id?'is-active':''}" data-candidate-id="${esc(item.id)}"><img src="${esc(item.url)}" alt="候選圖片 ${index+1}"><span class="script-artwork-list-copy"><strong>候選圖片 ${index+1}</strong><small>${item.width} × ${item.height}</small></span><span class="script-artwork-role ${kind?'is-assigned':''}" title="${kind?artworkKinds.find(([key])=>key===kind)?.[1]:'尚未指定'}">${artworkIcon(kind)}</span><span class="script-artwork-remove" data-artwork-remove title="從本次候選清單移除" aria-label="刪除候選圖片 ${index+1}"><i class="fas fa-times" aria-hidden="true"></i></span></button>`;}).join('')}</aside></div>`;}
  function bindArtworkPicker(host,selection){if(!host)return;host.onclick=(event)=>{const remove=event.target.closest('[data-artwork-remove]'),item=event.target.closest('[data-candidate-id]'),assign=event.target.closest('[data-artwork-kind]');if(remove&&item){event.preventDefault();event.stopPropagation();const candidateId=item.dataset.candidateId;state.artworkCandidates=state.artworkCandidates.filter((candidate)=>candidate.id!==candidateId);for(const key of Object.keys(selection)){if(selection[key]===candidateId)selection[key]='';}if(state.activeArtworkId===candidateId)state.activeArtworkId=state.artworkCandidates[0]?.id||'';}else if(item){state.activeArtworkId=item.dataset.candidateId;}else if(assign){const candidateId=state.activeArtworkId,kind=assign.dataset.artworkKind;if(!candidateId)return;for(const key of Object.keys(selection)){if(selection[key]===candidateId)selection[key]='';}selection[kind]=candidateId;}host.innerHTML=artworkPicker(state.artworkCandidates,selection);bindArtworkPicker(host,selection);};}
  function imageEditor(images){const bySlot=new Map((images||[]).map((item)=>[Number(item.sort_order),item]));const slots=[[0,'正面'],[1,'背面'],[100,'Logo']];return `<section class="script-image-editor"><div class="script-custom-title"><div><h4>劇本圖片</h4><small>可手動補傳，或從鐘樓圖書館文章載入全部候選圖片後指定用途；儲存後會下載到站內。</small></div><div class="script-image-head-actions"><button id="script-artwork-load" type="button" class="btn btn-outline">從原文載入候選圖</button><button id="script-images-save" type="button" class="btn btn-purple">儲存手動上傳</button></div></div><div class="script-image-slots">${slots.map(([slot,label])=>{const item=bySlot.get(slot);return `<label class="script-image-slot" data-image-slot="${slot}"><strong>${label}</strong><div class="script-image-preview ${slot===100?'is-logo':''}">${item?.url?`<img src="${esc(item.url)}" alt="${esc(item.alt||label)}">`:`<span>尚未上傳${label}</span>`}</div><input type="file" accept="image/*" class="form-control dark-input" data-script-image-file><small data-image-name>${item?'選擇檔案即可替換':'請選擇圖片檔案'}</small></label>`;}).join('')}</div><div id="script-artwork-review" class="script-artwork-review" hidden><div class="script-custom-title"><div><h4>文章候選圖片</h4><small>逐張指定正面、背面或 Logo；同一張圖只能有一種用途。</small></div><button id="script-artwork-apply" type="button" class="btn btn-purple">套用選取</button></div><div id="script-artwork-picker"></div></div><div id="script-images-status" class="role-admin-status"></div></section>`;}  function customCard(x){return `<article class="script-custom-card" data-custom-id="${x.id||''}"><div class="script-custom-head"><img src="${esc(x.image_url||'/icons/default-role.svg')}" alt=""><strong>${esc(x.name_zh_tw||'新條目')}</strong><small>${esc(typeLabel(x.team))}</small><button type="button" class="btn btn-outline script-custom-delete" data-custom-delete>刪除</button></div><div class="script-custom-grid"><label>名稱<input class="form-control dark-input" data-custom-name value="${esc(x.name_zh_tw||'')}"></label><label>分類<select class="form-control dark-input" data-custom-team>${typeOptions(x.team||'special')}</select></label><label class="wide">圖片網址<input class="form-control dark-input" data-custom-image value="${esc(x.image_url||'')}"></label><label class="wide">角色能力<textarea class="form-control dark-input" data-custom-ability data-rich-text>${esc(x.ability||'')}</textarea></label></div></article>`;}
  function customGroups(items){return customTypes.map(([key,label])=>{const entries=items.filter((x)=>(x.team||'special')===key);return entries.length?`<section class="script-custom-group" data-custom-group="${key}"><h5>${label}（${entries.length}）</h5>${entries.map(customCard).join('')}</section>`:'';}).join('');}
  function bindCustomActions(){document.querySelectorAll('[data-custom-delete]').forEach((button)=>button.onclick=()=>button.closest('[data-custom-id]').remove());$('script-custom-add').onclick=()=>{const host=$('script-custom-list');host.querySelector('.role-admin-empty')?.remove();let group=host.querySelector('[data-custom-group="special"]');if(!group){group=document.createElement('section');group.className='script-custom-group';group.dataset.customGroup='special';group.innerHTML='<h5>其他</h5>';host.appendChild(group);}group.insertAdjacentHTML('beforeend',customCard({team:'special'}));bindCustomActions();document.dispatchEvent(new CustomEvent('rich-text-refresh'));};}
  async function load(id){const host=$('script-admin-detail');host.innerHTML='<div class="role-admin-empty">正在讀取…</div>';try{state.active=await request('/api/admin/scripts/'+id);renderList();const s=state.active;host.innerHTML=`<div class="article-admin-head"><div><h3>${esc(s.name_zh_tw)}</h3><small>${esc(s.slug)}</small></div><a class="btn btn-outline" href="/#scripts/${encodeURIComponent(s.slug)}">公開頁面</a></div><div class="script-admin-meta"><label>劇本名稱<input id="sam-name" class="form-control dark-input" value="${esc(s.name_zh_tw)}"></label><label>版本<input id="sam-version" class="form-control dark-input" value="${esc(s.version||'')}"></label><label>作者<input id="sam-author" class="form-control dark-input" value="${esc(s.author_name||'')}"></label><label>&#20998;&#39006;<select id="sam-category" class="form-control dark-input">${categoryOptions(s.category||'')}</select></label><label>標籤（逗號分隔）<input id="sam-tags" class="form-control dark-input" value="${esc((s.tags||[]).join('，'))}"></label><label>原文連結<input id="sam-source" class="form-control dark-input" value="${esc(s.source_url||'')}"></label><div class="script-admin-flags"><label class="script-admin-flag"><input id="sam-public" type="checkbox" ${s.is_public?'checked':''}> &#20844;&#38283;</label><label class="script-admin-flag"><input id="sam-review" type="checkbox" ${s.needs_review?'checked':''}> &#24453;&#23529;&#26680;</label><label class="script-admin-flag"><input id="sam-laplace" type="checkbox" ${s.is_laplace_owned?'checked':''}> &#25289;&#26222;&#25289;&#26031;&#26377;&#27492;&#21127;&#26412;</label></div></div>${imageEditor(s.images)}<div class="script-admin-copy">${field('一句話介紹','sam-tagline',s.tagline,true,true)}${field('劇本介紹','sam-intro',s.introduction,true,true)}${field('劇本背景介紹','sam-background',s.background_introduction,true,true)}${field('核心體驗與玩法特色','sam-gameplay',s.gameplay_overview,true,true)}${field('作者的話','sam-author-note',s.author_note,true,true)}${field('製作與更新資訊','sam-updates',s.production_updates,true,true)}${field('玩家攻略','sam-player-guide',s.player_guide,true,true)}${field('說書人攻略（登入後可見）','sam-storyteller-guide',s.storyteller_guide,true,true)}</div><section class="script-json-import"><h4>匯入角色 JSON</h4><p>先預覽官方角色與自創／特殊條目的配對結果；確認無遺漏後才會替換目前角色構成。</p><div class="script-json-actions"><input id="script-role-json-file" type="file" accept=".json,application/json" class="form-control dark-input"><button id="script-role-json-preview" class="btn btn-outline" type="button">預覽配對</button><button id="script-role-json-apply" class="btn btn-purple" type="button" disabled>確認套用</button></div><div id="script-role-json-report" class="role-admin-status">尚未選擇 JSON。</div></section><h4>官方角色（${s.roles.length}）</h4><div class="script-role-summary">${s.roles.map((r)=>`<span><img src="${esc(r.image_url||'/icons/default-role.svg')}">${esc(r.name_zh_tw)}</span>`).join('')}</div><div class="script-custom-title"><h4>自創與特殊角色（${s.custom_roles.length}）</h4><button id="script-custom-add" type="button" class="btn btn-outline">新增條目</button></div><div id="script-custom-list">${customGroups(s.custom_roles)||'<div class="role-admin-empty">沒有自創或特殊角色。</div>'}</div><div class="article-admin-block-actions script-admin-primary-actions"><button id="script-admin-delete" class="btn script-danger-button" type="button"><i class="fas fa-trash-alt"></i> &#21034;&#38500;&#21127;&#26412;</button><button id="script-admin-save" class="btn btn-purple">儲存劇本</button></div>`;$('script-admin-save').onclick=save;$('script-admin-delete').onclick=deleteScript;$('script-images-save').onclick=saveImages;$('script-artwork-load').onclick=loadArtworkCandidates;$('script-artwork-apply').onclick=applyArtworkSelection;document.querySelectorAll('[data-script-image-file]').forEach((input)=>input.onchange=()=>previewImageFile(input));bindCustomActions();$('script-role-json-preview').onclick=previewRoleJson;$('script-role-json-apply').onclick=applyRoleJson;document.dispatchEvent(new CustomEvent('rich-text-refresh'));}catch(e){host.innerHTML='<div class="role-admin-empty">讀取失敗：'+esc(e.message)+'</div>';}}
  async function loadArtworkCandidates(){const status=$('script-images-status'),review=$('script-artwork-review'),picker=$('script-artwork-picker');status.textContent='正在下載文章候選圖片…';try{const data=await request(`/api/admin/scripts/${state.active.id}/artwork-candidates`,{method:'POST'});state.artworkCandidates=data.items||[];state.artworkSelection={front:'',back:'',logo:''};picker.innerHTML=artworkPicker(state.artworkCandidates,state.artworkSelection);bindArtworkPicker(picker,state.artworkSelection);review.hidden=false;status.textContent=state.artworkCandidates.length?`已載入 ${state.artworkCandidates.length} 張候選圖片，請指定用途。`:'文章內沒有可用圖片。';}catch(e){status.textContent='候選圖片載入失敗：'+e.message;}}
  async function applyArtworkSelection(){const status=$('script-images-status');if(!state.artworkSelection.front||!state.artworkSelection.back){status.textContent='請至少指定正面與背面。';return;}status.textContent='正在下載並套用選取圖片…';try{const data=await request(`/api/admin/scripts/${state.active.id}/artwork-selection`,{method:'POST',body:JSON.stringify({artwork_selection:state.artworkSelection})});await load(data.script.id);$('script-admin-status').textContent='已更新 '+data.script.name_zh_tw+' 的正面、背面與 Logo';}catch(e){status.textContent='套用失敗：'+e.message;}}  async function readRoleJson(){const input=$('script-role-json-file'),file=input?.files?.[0];if(!file)throw new Error('請先選擇劇本 JSON');const content=await file.text();state.roleJson={filename:file.name,content};return state.roleJson;}
  function renderRoleJsonReport(data,{applied=false}={}){const missing=(data.roles_missing||[]).map((x)=>x.name||x.id).join('、');const duplicates=(data.duplicate_entries_ignored||[]).length;const host=$('script-role-json-report');const success=applied?'<br><span class="text-success">已完整取代目前角色構成。</span>':'<br><span class="text-success">預覽完成，尚未寫入。請按「確認套用」完整取代目前角色構成。</span>';host.innerHTML=`共 ${data.entries_found} 個條目：<strong>${data.roles_matched}</strong> 個官方角色、<strong>${data.special_entries_preserved}</strong> 個自創／特殊條目${duplicates?`、忽略 ${duplicates} 個重複條目`:''}${missing?`<br><span class="text-danger">無法辨識：${esc(missing)}</span>`:success}`;const button=$('script-role-json-apply');button.disabled=applied||!data.can_apply;button.textContent=applied?'已套用':'確認套用並取代目前角色';}
  async function previewRoleJson(){try{const payload=await readRoleJson();const data=await request(`/api/admin/scripts/${state.active.id}/role-json/preview`,{method:'POST',body:JSON.stringify(payload)});renderRoleJsonReport(data);}catch(e){$('script-role-json-report').textContent='預覽失敗：'+e.message;$('script-role-json-apply').disabled=true;}}
  async function applyRoleJson(){if(!state.roleJson)return;const button=$('script-role-json-apply');button.disabled=true;button.textContent='套用中…';try{const data=await request(`/api/admin/scripts/${state.active.id}/role-json/apply`,{method:'POST',body:JSON.stringify(state.roleJson)});state.active=data.script;renderRoleJsonReport(data,{applied:true});$('script-admin-status').textContent=`已更新 ${data.script.name_zh_tw}：${data.script.roles.length} 個官方角色、${data.script.custom_roles.length} 個自創／特殊條目`;await load(data.script.id);}catch(e){$('script-role-json-report').textContent='套用失敗：'+e.message;button.disabled=false;button.textContent='確認套用並取代目前角色';}}
  async function deleteScript(){const s=state.active;if(!s)return;const confirmed=window.confirm('確定刪除「'+s.name_zh_tw+'」？\n\n劇本、圖片紀錄、角色關聯與自創／特殊條目會一併移除。');if(!confirmed)return;const typed=window.prompt('請輸入劇本名稱「'+s.name_zh_tw+'」確認刪除：','');if(typed!==s.name_zh_tw){if(typed!==null)window.alert('名稱不一致，未刪除劇本。');return;}try{const result=await request('/api/admin/scripts/'+s.id,{method:'DELETE'});state.active=null;state.roleJson=null;$('script-admin-detail').innerHTML='<div class="role-admin-empty">劇本已刪除。可重新匯入或選擇其他劇本。</div>';$('script-admin-status').textContent='已刪除 '+result.deleted_name;await refresh();}catch(e){$('script-admin-status').textContent='刪除失敗：'+e.message;}}
  function previewImageFile(input){const file=input.files?.[0],slot=input.closest('[data-image-slot]');if(!file||!slot)return;const preview=slot.querySelector('.script-image-preview'),name=slot.querySelector('[data-image-name]');const url=URL.createObjectURL(file);preview.innerHTML=`<img src="${url}" alt="待上傳預覽">`;name.textContent=`待上傳：${file.name}`;}
  async function saveImages(){const button=$('script-images-save'),status=$('script-images-status'),uploads=[];for(const slot of document.querySelectorAll('[data-image-slot]')){const file=slot.querySelector('[data-script-image-file]').files?.[0];if(file)uploads.push({slot:Number(slot.dataset.imageSlot),...(await fileData(file))});}if(!uploads.length){status.textContent='請先選擇要補傳或替換的正面／背面圖片。';return;}button.disabled=true;status.textContent='正在保存圖片…';try{const result=await request(`/api/admin/scripts/${state.active.id}/images`,{method:'POST',body:JSON.stringify({images:uploads})});state.active=result.script;await load(result.script.id);$('script-admin-status').textContent='已更新 '+result.script.name_zh_tw+' 的劇本圖片';}catch(e){status.textContent='圖片儲存失敗：'+e.message;button.disabled=false;}}
  async function save(){const s=state.active,custom=[...document.querySelectorAll('[data-custom-id]')].map((el)=>({id:el.dataset.customId?Number(el.dataset.customId):null,name_zh_tw:el.querySelector('[data-custom-name]').value,team:el.querySelector('[data-custom-team]').value,image_url:el.querySelector('[data-custom-image]').value,ability:el.querySelector('[data-custom-ability]').value}));const payload={name_zh_tw:$('sam-name').value,version:$('sam-version').value,author_name:$('sam-author').value,category:$('sam-category').value,tags:$('sam-tags').value.split(/[，,]/).map((x)=>x.trim()).filter(Boolean),source_url:$('sam-source').value,is_public:$('sam-public').checked,needs_review:$('sam-review').checked,is_laplace_owned:$('sam-laplace').checked,tagline:$('sam-tagline').value,introduction:$('sam-intro').value,background_introduction:$('sam-background').value,gameplay_overview:$('sam-gameplay').value,author_note:$('sam-author-note').value,production_updates:$('sam-updates').value,player_guide:$('sam-player-guide').value,storyteller_guide:$('sam-storyteller-guide').value,custom_roles:custom};try{const result=await request('/api/admin/scripts/'+s.id,{method:'PATCH',body:JSON.stringify(payload)});state.active=result.script;$('script-admin-status').textContent='已儲存 '+state.active.name_zh_tw;await refresh();await load(s.id);}catch(e){$('script-admin-status').textContent='儲存失敗：'+e.message;}}
  function newImportForm(){state.active=null;renderList();state.importPayload=null;state.artworkCandidates=[];state.artworkSelection={front:'',back:'',logo:''};const host=$('script-admin-detail');host.innerHTML='<div class="article-admin-head"><div><h3>新增劇本</h3><small>先預覽角色配對與文章候選圖片，確認後才建立內部待審核草稿。</small></div></div><div class="script-import-grid"><label class="wide">鐘樓博物館網址（選填）<input id="si-source" class="form-control dark-input" placeholder="https://www.bilibili.com/opus/..."></label><label>劇本名稱（手動上傳時必填）<input id="si-name" class="form-control dark-input"></label><label>作者<input id="si-author" class="form-control dark-input"></label><label>版本<input id="si-version" class="form-control dark-input"></label><label>分類<select id="si-category" class="form-control dark-input"><option value="官方劇本">官方劇本</option><option value="官混劇本" selected>官混劇本</option><option value="部分原創">部分原創</option><option value="完全原創">完全原創</option></select></label><label><input id="si-laplace" type="checkbox"> 拉普拉斯有此劇本</label><label class="wide">一句話介紹<textarea id="si-tagline" class="form-control dark-input"></textarea></label><label class="wide">劇本介紹<textarea id="si-intro" class="form-control dark-input"></textarea></label><label>正面（手動備援）<input id="si-front" type="file" accept="image/*" class="form-control dark-input"></label><label>背面（手動備援）<input id="si-back" type="file" accept="image/*" class="form-control dark-input"></label><label>Logo（選填）<input id="si-logo" type="file" accept="image/*" class="form-control dark-input"></label><label>劇本 JSON（角色名單唯一權威）<input id="si-json" type="file" accept=".json,application/json" class="form-control dark-input"></label></div><div class="script-import-actions"><button id="si-preview" type="button" class="btn btn-outline">預覽文章、圖片與角色配對</button><button id="si-apply" type="button" class="btn btn-purple" disabled>確認建立草稿</button></div><div id="si-report" class="script-import-preview" hidden></div>';$('si-preview').onclick=previewNewImport;$('si-apply').onclick=applyNewImport;}  function fileData(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve({filename:file.name,content_type:file.type,content:String(reader.result).split(',',2)[1]||''});reader.onerror=()=>reject(reader.error);reader.readAsDataURL(file);});}
  async function collectNewImport(){const jsonFile=$('si-json').files?.[0];if(!jsonFile)throw new Error('請上傳劇本 JSON');const images=[];for(const [slot,id] of [[0,'si-front'],[1,'si-back'],[100,'si-logo']]){const file=$(id).files?.[0];if(file)images.push({slot,...(await fileData(file))});}return {source_url:$('si-source').value.trim(),name_zh_tw:$('si-name').value.trim(),author_name:$('si-author').value.trim(),version:$('si-version').value.trim(),category:$('si-category').value.trim(),is_laplace_owned:$('si-laplace').checked,tagline:$('si-tagline').value.trim(),introduction:$('si-intro').value.trim(),role_json:await jsonFile.text(),images};}  function showImportReport(data){const metadata=data.metadata||{},missing=(data.roles_missing||[]).map((x)=>x.name||x.id).join('、'),special=(data.special_entries||[]).map((x)=>(x.name||x.id)+'（'+typeLabel(x.team)+'）').join('、');state.artworkCandidates=data.artwork_candidates||[];state.artworkSelection={front:'',back:'',logo:''};const host=$('si-report');host.hidden=false;host.innerHTML='<h4>'+esc(metadata.name_zh_tw||$('si-name').value||'未命名劇本')+'</h4><p>作者：'+esc(metadata.author_name||$('si-author').value||'待補')+'；圖片：'+(data.uploaded_images?data.uploaded_images+' 張手動上傳':state.artworkCandidates.length+' 張文章候選')+'</p><p>配對官方角色 <strong>'+data.roles_matched+'</strong>；保留自創與特殊條目 <strong>'+data.special_entries_preserved+'</strong>：'+(special?esc(special):'無')+'</p>'+(missing?'<p class="text-danger">尚未辨識：'+esc(missing)+'</p>':'<p class="text-success">角色均已辨識；建立後仍會保持內部、待審核。</p>')+(state.artworkCandidates.length&&!data.uploaded_images?'<div class="script-custom-title"><div><h4>文章候選圖片</h4><small>請明確指定正面、背面與選填的 Logo。</small></div></div><div id="si-artwork-picker">'+artworkPicker(state.artworkCandidates,state.artworkSelection)+'</div>':'');if(state.artworkCandidates.length&&!data.uploaded_images)bindArtworkPicker($('si-artwork-picker'),state.artworkSelection);$('si-apply').disabled=!data.can_apply;}  async function previewNewImport(){try{state.importPayload=await collectNewImport();const data=await request('/api/admin/scripts/imports/preview',{method:'POST',body:JSON.stringify(state.importPayload)});showImportReport(data);}catch(e){const host=$('si-report');host.hidden=false;host.textContent='預覽失敗：'+e.message;$('si-apply').disabled=true;}}
  async function applyNewImport(){if(!state.importPayload)return;if(!state.importPayload.images?.length&&state.artworkCandidates.length){if(!state.artworkSelection.front||!state.artworkSelection.back){$('si-report').insertAdjacentHTML('afterbegin','<p class="text-danger">請先指定正面與背面。</p>');return;}state.importPayload.artwork_selection={...state.artworkSelection};state.importPayload.artwork_candidates=state.artworkCandidates;}try{$('si-apply').disabled=true;$('si-report').textContent='正在保存圖片、角色配對與劇本資料…';const data=await request('/api/admin/scripts/imports/apply',{method:'POST',body:JSON.stringify(state.importPayload)});$('script-admin-status').textContent='已建立內部待審核草稿：'+data.script.name_zh_tw;await refresh();await load(data.script.id);}catch(e){$('si-report').textContent='建立失敗：'+e.message;$('si-apply').disabled=false;}}  function init(){document.querySelectorAll('[data-admin-mode]').forEach((b)=>b.addEventListener('click',()=>switchMode(b.dataset.adminMode)));$('script-admin-refresh')?.addEventListener('click',refresh);$('script-admin-create')?.addEventListener('click',newImportForm);$('script-admin-search')?.addEventListener('keydown',(e)=>{if(e.key==='Enter')refresh();});}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();
