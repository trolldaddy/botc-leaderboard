name: Seed Initial Knowledge Blocks

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Preview or write the first public blocks"
        required: true
        default: "preview"
        type: choice
        options:
          - preview
          - write

permissions:
  contents: read

jobs:
  seed:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/town-checkin

      - name: Connect to Cloud SQL
        uses: ./.github/actions/setup-cloud-sql
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Seed blocks
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ inputs.mode }}" = "write" ]; then
            python scripts/seed_initial_knowledge_blocks.py --write
          else
            python scripts/seed_initial_knowledge_blocks.py
          fi
